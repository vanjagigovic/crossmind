# Defaults to /api, which Vite proxies to VITE_BACKEND_URL during development.
VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:3000
# Optional fallback when no ?puzzleId=<id> query parameter is provided.
VITE_PUZZLE_ID=